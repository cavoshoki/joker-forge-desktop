[package]
name = "balatro-codegen"
version = "0.1.0"
edition = "2021"
description = "Lua code generation for Balatro mods"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
